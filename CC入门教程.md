# CC（Claude Code）入门教程

> 面向第一次接触 Claude Code 的新手。看完这篇，你就能用它帮你写代码、改 Bug、做各种工程任务。

---

## 1. Claude Code 是什么

Claude Code 是 Anthropic 官方的**命令行 AI 编程助手**。它不是普通的聊天窗口，而是一个能在你的电脑上「动手干活」的代理：

- 能**读**你的代码库，理解项目结构；
- 能**写 / 改**文件（在你授权后）；
- 能**执行终端命令**（构建、测试、git 等）；
- 能自己**规划**多步骤任务，一步步完成。

它运行在终端里，也有桌面版（Mac / Windows）、网页版（claude.ai/code）和 IDE 插件（VS Code、JetBrains）。

一句话理解：**你给它下指令，它在你的项目里实打实地帮你把活干了。**

---

## 2. 安装

### 方式一：npm 全局安装（推荐，最通用）

需要先装好 Node.js（≥ 18）。

```bash
npm install -g @anthropic-ai/claude-code
```

装完直接运行：

```bash
claude
```

### 方式二：桌面应用 / IDE 插件

- 桌面版：Mac / Windows 从官网下载安装。
- VS Code / JetBrains：在扩展市场搜「Claude Code」安装。

> 无论哪种方式，第一次启动都会让你登录 Anthropic 账号（或配置 API Key）。

---

## 3. 第一次对话

启动后，直接在输入框里说人话即可。例如：

```
帮我看一下这个项目的结构，它主要做了什么？
```

```
帮我写一个读取 CSV 文件并统计每列平均值的 Python 脚本。
```

Claude Code 会：

1. 先**读代码 / 查资料**（用只读工具）；
2. 需要改动时**征求你的同意**（取决于权限模式）；
3. 执行后**报告结果**，失败会老实说。

---

## 4. 核心概念

### 4.1 权限模式

Claude Code 对「改动文件 / 跑命令」这类操作有权限控制。默认会询问你。你可以：

- 每次确认（默认，最安全）；
- 用快捷键 / 命令放行某类操作；
- 在项目里配置 `.claude/settings.json` 提前授权常用命令。

### 4.2 CLAUDE.md —— 你的「项目说明书」

在项目根目录放一个 `CLAUDE.md`，Claude Code 每次启动都会读它。你可以在里面写：

- 项目是干什么的、怎么构建运行；
- 代码风格约定；
- 你的特殊要求（比如「所有回复用中文」）。

个人全局的规则放在 `~/.claude/CLAUDE.md`（对所有项目生效）。

### 4.3 Memory（记忆）

Claude Code 有持久化记忆，存放在 `~/.claude/projects/<项目>/memory/`。你可以让它「记住」：

- 你的身份、偏好；
- 你纠正过它的做法；
- 项目正在进行的长期工作。

这样下次会话它还能记得。

---

## 5. 常用功能

### 5.1 Slash 命令（`/` 开头）

| 命令 | 作用 |
| --- | --- |
| `/help` | 查看帮助 |
| `/clear` | 清空当前对话上下文 |
| `/compact` | 压缩对话历史（上下文太长时用） |
| `/config` | 配置主题、模型、权限等 |
| `/memory` | 管理记忆 |
| `/init` | 为当前项目生成 CLAUDE.md |

### 5.2 Skills（技能）

Skill 是一套「针对某类任务的打包指令」，让 Claude Code 在某件事上做得更专业。比如：

- `frontend-design`：做前端视觉设计时用；
- `skill-creator`：创建新的 skill；
- `find-skills`：帮你发现和安装 skill。

用 `/技能名` 调用。你项目里的技能放在 `.claude/skills/` 目录。

### 5.3 计划模式（Plan Mode）

对复杂、多文件、有多种做法的任务，先让 Claude Code **探索代码、产出方案、等你确认**，再动手改。避免一上来就改错方向。

### 5.4 子代理（Subagents）

Claude Code 可以派「子代理」并行去做多件事（比如同时查多个文件、同时跑几个搜索），它只把结论带回来，不把一堆中间过程倒给你。

---

## 6. 进阶

- **MCP**：接外部工具（数据库、浏览器、第三方 API），扩展能力。
- **Hooks**：在特定事件（如每次工具调用前后）自动执行脚本，实现自动化流程。
- **Workflows**：编排多个子代理做确定性的大规模任务。

这些入门阶段不用急，先把手感和前 5 节用好。

---

## 7. 实战：一个番茄钟项目是怎么用 CC 做的

以我们自己的 `first-cc` 项目为例，一个典型流程：

1. **下指令**：「帮我优化番茄钟的 UI」；
2. CC **读文件**：先读 `番茄钟.html` 和 `品牌视觉规范`，理解现状；
3. CC **套规则**：发现项目 CLAUDE.md 要求「改前端视觉要参考品牌视觉规范」，于是按那 6 条规则（粗黑描边、手绘环绕线、半透明果冻感等）改；
4. CC **加 skill**：你后来加了 `frontend-design` skill，它又按 skill 的设计原则再优化一版；
5. 你在浏览器**看效果**，不满意就继续反馈，CC 接着调。

这就是 Claude Code 的核心工作方式：**读 → 想 → 改 → 汇报 → 根据反馈迭代。**

---

## 8. 新手小贴士

- **指令说具体点**：与其说「改一下」，不如说「把按钮颜色改成蓝色、圆角调大」。
- **让它先看再改**：复杂任务可以先说「先别改，帮我看看这个功能是怎么实现的」。
- **别怕报错**：CC 跑测试失败会如实告诉你，附上输出，你可以让它继续修。
- **善用 CLAUDE.md 和 Memory**：把反复强调的要求写下来，省得每次重说。

---

> 祝顺利！有问题继续问它就行。
